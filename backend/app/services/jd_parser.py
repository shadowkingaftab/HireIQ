from typing import Dict, Any

class JdParser:
    def parse(self, *, jd_text: str) -> Dict[str, Any]:
        # Placeholder for AI-powered JD parsing logic
        return {
            "required_skills": ["python", "sql"],
            "preferred_skills": ["docker", "aws"],
            "minimum_experience": 3,
            "location": "Remote"
        }

jd_parser = JdParser()
