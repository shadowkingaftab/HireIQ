from typing import List, Dict, Any

class SkillMapper:
    def map_to_skills(self, *, text_content: str) -> List[str]:
        # Extract skills from text using NLP or ontology mapping
        return []

skill_mapper = SkillMapper()
